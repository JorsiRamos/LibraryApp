<h1>Contact Us</h1>

<div class="form-horizontal form-group form-group-lg row">

    <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2">
       {{input type="email" value=emailContact class="form-control" placeholder="Type your email" autofocus="autofocus"}}
    </div>
    <div class="col-xs-10 col-xs-offset-1 col-sm-6 col-sm-offset-1 col-md-5 col-md-offset-2">
    	{{textarea class="form-control" placeholder="Type your message. (At least 5 characters.)" rows="7" value=userMessage}}
    </div>	
    <div class="col-xs-6 col-xs-offset-1 col-md-5 col-md-offset-2 contact-btn">
		<button class="btn btn-primary btn-lg btn-block" disabled="{{unless isValid 'disabled'}}" {{action 'sendContact'}}>Send</button>
    </div>

</div>

{{#if messageSend}}
    <div class="alert alert-success">{{messageSend}}</div>
{{/if}} 

<br/><br/>